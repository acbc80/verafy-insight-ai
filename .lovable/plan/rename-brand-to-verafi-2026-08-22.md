# Rename brand to VeraFi

Change every user-visible occurrence of "Verafy" to "VeraFi".

## Changes

- `index.html`: page title, meta description/author, Open Graph and Twitter titles.
- `src/pages/Index.tsx`: header wordmark next to the logo tile.
- `src/pages/CompanyProfile.tsx`: header wordmark in the sticky nav.

The logo tile keeps the single "V" mark, so no asset changes are needed. No database, styling, or layout changes.

## Notes

Published URL slug stays as-is unless you want it renamed too.
